import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    action: {},
    name: "firefox-profile-switcher",
    version: "1",
  },
  manifestVersion: 3,
  vite: () => ({}),
});
